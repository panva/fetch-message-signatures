# Type Alias: NormalizedRequest

> **NormalizedRequest** = [`SignableRequest`](SignableRequest.md) & `object`

A [SignableRequest](SignableRequest.md) whose fields use the host's `Headers` representation.

## Type Declaration

### headers

> `readonly` **headers**: `Headers`
